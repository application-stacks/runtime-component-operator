apiVersion: rc.app.stacks/v1
kind: RuntimeComponent
metadata:
  name: statefulset-strategy-rc
spec:
  # Add fields here
  replicas: 3
  statefulSet:
    updateStrategy:
      rollingUpdate: null
      type: OnDelete