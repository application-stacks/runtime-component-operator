apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: network-policy-rc
spec:
  ingress:
    - from:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              foo: bar
      ports:
        - port: 8080