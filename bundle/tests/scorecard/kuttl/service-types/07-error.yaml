apiVersion: v1
kind: Service
metadata:
  name: service-types-rc
  annotations:
    name: NodePort
spec:
  ports:
    - name: test-port
      protocol: TCP
      port: 8080
      targetPort: 8081
      nodePort: 30932