apiVersion: rc.app.stacks/v1
kind: RuntimeComponent
metadata:
  name: init-runtime-deployment
spec:
  # Add fields here
  initContainers:
  - image: alpine:3.14
    command: ["echo"]
    args: ["Hello world"]
    name: init1

