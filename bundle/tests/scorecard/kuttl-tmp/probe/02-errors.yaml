apiVersion: v1
kind: Pod
spec:
  containers:
    - readinessProbe:
        initialDelaySeconds: 3
      livenessProbe:
        initialDelaySeconds: 6
      startupProbe:
        failureThreshold: 20
        periodSeconds: 10
