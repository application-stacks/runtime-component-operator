scorecard:
  # Setting a global scorecard option
  output: json
  bundle: deploy/olm-catalog/runtime-component-operator-certified
  plugins:
    # `basic` tests configured to test 2 CRs
    - basic:
        cr-manifest:
          - "deploy/crds/app.stacks_runtimecomponents_cr.yaml"
    # `olm` tests configured to test 2 CRs
    - olm:
        cr-manifest:
          - "deploy/crds/app.stacks_runtimecomponents_cr.yaml"
        csv-path: "deploy/olm-catalog/runtime-component-operator-certified/runtime-component-operator.v0.7.0.clusterserviceversion.yaml"
